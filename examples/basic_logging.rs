use loggix::{debug, error, info, warn};

fn main() {
    // Basic logging with different levels
    debug!("Debug message");
    info!("Info message");
    warn!("Warning message");
    error!("Error message");
}
