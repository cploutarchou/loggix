use loggix::{info, debug, warn, error};

fn main() {
    // Basic logging with different levels
    debug!("Debug message");
    info!("Info message");
    warn!("Warning message");
    error!("Error message");
}
